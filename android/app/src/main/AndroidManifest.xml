<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yexx.pokeagenda">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PokeAgenda">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Detalhe" />
        <!-- Activities Menu -->
        <activity android:name=".ConsultarPokemon"
            android:label="@string/menu_primeira_opcao"
            android:parentActivityName=".WelcomeActivity" />
        <activity android:name=".CadastrarPokemon"
            android:label="@string/menu_segunda_opcao"
            android:parentActivityName=".WelcomeActivity"/>
        <activity android:name=".PesquisarPokemon"
            android:label="@string/menu_terceira_opcao"
            android:parentActivityName=".WelcomeActivity"/>
        <!-- - -->
        <activity android:name=".WelcomeActivity"></activity>
    </application>

</manifest>